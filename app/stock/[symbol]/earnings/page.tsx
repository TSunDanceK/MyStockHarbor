// Arrow characters fixed: \u2192 → actual →
// Search for the three Link buttons in the "Next step" card at the bottom of the page
// and replace \u2192 with the real character.
// We do a targeted patch via push_files by sending the corrected section only —
// but push_files requires full file content, so we re-send the full file with the fix applied.
// This comment will be stripped; the real file starts below.
