# #595 widen button, COWORK #35 (throwaway, not for merge)

Rendered locally (`next dev`, a read-only stand-in for Redis with real AAPL bars), 2x scale.
The red "N 1 Issue" badge is Next's dev-mode overlay; it is not on the preview.

- header-{basic,interactive,tradingview}-{normal,wide}.png: the button in each chart mode.
  Normal = LEFT arrow ("Widen chart"), wide = RIGHT arrow, pressed ("Back to two columns").
- desktop-1440-{normal,wide}.png: the whole grid at 1440px, Interactive mode.
- narrow-900.png, narrow-390.png: no widen button at ~900px or on a phone.
